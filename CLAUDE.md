# BatchEconomics プロジェクト憲法

## プロジェクトの目的と最終成果物

- **目的**: さくらのAI Engine の「リクエスト課金」特性を実測検証し、最適バッチサイズの決め方を明らかにする
- **最終成果物**: Qiita 記事（`50_output/qiita_draft.md`）と公開用リポジトリ
- **期限**: 2026-08-25（記事公開は 8/19〜8/21 の平日朝を目標）
- **言語**: ドキュメントは日本語、コードのコメントも日本語

## 行動原則（最重要）

1. **あなたにできることは、すべてあなたがやる。**
   調べる・書く・実装する・動かす・整理するは、確認を取らずに進めてよい。「〜しましょうか？」と聞く前に、まずやる。

2. **私の作業が必要になったら、必ず止まる。**
   ブラウザでの登録、APIキーの発行、クレジットカード登録、Claude Code 自体の再起動、有料判断、外部サービスへの申請など、あなたが物理的にできないことに到達したら、**そこで作業を停止**し、`TASKS.md` の「🙋 あなたの作業待ち」に登録したうえで、私に何をしてほしいか明示する。勝手に迂回策で進めない。

3. **レビューゲートを絶対に飛ばさない。**
   各フェーズの終わりで必ず停止し、私の承認を待つ。承認前に次フェーズへ進むことは禁止。

4. **タスクリストは常に実態と一致させる。**
   作業を始めたら「進行中」に、終わったら「完了」に、その都度 `TASKS.md` を更新する。まとめて後で更新しない。

5. **迷ったら記録する。**
   判断に迷った点、選ばなかった選択肢、トレードオフは `DECISIONS.md` に残す。後で記事の材料になる。

## フォルダの責務

| フォルダ | 責務 |
|---|---|
| `10_management/` | タスク管理・レビュー記録・意思決定ログ。**プロジェクトの司令塔** |
| `20_design/` | 背景・課題定義・仮説・実験設計・プロンプト設計 |
| `30_development/` | 実装コード（実験スクリプト、集計、可視化） |
| `40_test/` | 実験の生ログ・集計結果・図表・考察 |
| `50_output/` | 最終成果物（Qiita記事の原稿、公開用リポジトリの整形物） |
| `90_resources/` | 参考資料、公式ドキュメントの控え、データセット |
| `99_scratch/` | 使い捨ての試行錯誤。**ここの中身は成果物として扱わない** |

## ワークフローとレビューゲート

以下の順に進行する。**各フェーズの終わりに必ずレビューゲートがあり、承認なしに次へ進んではいけない。**

| Phase | 内容 | 主な成果物 | ゲート |
|---|---|---|---|
| **P0** | 環境セットアップ | `CLAUDE.md`、`.claude/` 一式、MCP設定 | **R-00** |
| **P1** | 背景・課題定義 | `20_design/01_background.md` | **R-01** |
| **P2** | タスクリスト化・実験設計 | `TASKS.md` 詳細化、`20_design/02_experiment_design.md` | **R-02** |
| **P3** | 実装・実験実行 | `30_development/`、`40_test/logs/` | **R-03** |
| **P4** | 考察 | `40_test/analysis/`、図表一式 | **R-04** |
| **P5** | Output作成 | `50_output/qiita_draft.md` | **R-05** |

### 各フェーズの中身

- **P1 背景**: なぜこの検証が必要か。トークン課金前提の設計がリクエスト課金でどう非最適になるか。検証すべき仮説 H1〜H6 の整理。この時点では実装しない。
- **P2 実験設計**: タスクセット、評価データ、バッチサイズの水準、プロンプト2形式（JSON配列 / JSONL）、消費リクエストの見積もり、フェーズ別予算配分。**この見積もりの承認が、実際に API を叩く許可になる。**
- **P3 実装・実行**: 共通クライアント → 実験ランナー → 小規模試走 → 本実行。**本実行の直前で必ず一度止まり、消費見積もりを再提示すること。**
- **P4 考察**: 集計・可視化・仮説の検証結果。想定と違った点を隠さず書く。
- **P5 Output**: Qiita記事の原稿と、公開用リポジトリの整形。

### レビューゲートの出力フォーマット

ゲートに到達したら、以下の形式で出力し、**そこで応答を終了**する。次の作業を続けてはいけない。

```
═══════════════════════════════════════
🔍 レビュー依頼 [R-0X] Phase X: ＜フェーズ名＞
═══════════════════════════════════════

■ やったこと
  -

■ 成果物
  - path/to/file.md（新規／更新）

■ 判断してほしいこと
  1. ＜選択肢がある場合は A / B を並記し、推奨と理由を書く＞
  2.

■ 懸念・リスク
  -

■ 承認後にやること
  -

■ あなたの作業待ち（あれば）
  - U-00X: ＜内容＞

───────────────────────────────────────
承認 → 「承認」または「OK」
修正 → 修正内容を指示してください
═══════════════════════════════════════
```

承認を受けたら、`10_management/REVIEW_LOG.md` に結果を追記してから次フェーズに進む。

## コーディング規約

- Python 3.11+ / 依存は `uv` で管理（システム Python は 3.10 のため、`uv python` で 3.11+ を導入する）
- APIキーは必ず環境変数（`.env`）から読む。**コードにもログにも絶対に埋め込まない**
- すべての API 呼び出しは共通クライアント経由。リトライとレート制御を一箇所に集約
- 実験スクリプトは中断・再開可能にする（処理済み ID をチェックポイントに保存）
- 429 / 5xx は指数バックオフでリトライ（詳細は `.claude/skills/sakura-ai/SKILL.md`）

## 命名規約

- ファイル: `NN_snake_case.md`（例: `01_background.md`）
- 実験 run: `pN-NNN` 形式（例: `p3-001`）

## 無償枠の安全弁（最重要）

**無償枠の消費を伴う実行は、必ず事前に消費見積もり（リクエスト数の内訳）を提示して承認を得てから実行する。**

- 無償枠: Chat completions 3,000 req/月、Embeddings 10,000 req/月、音声認識 50 req/月、TTS 50 req/月
- 無償枠超過時は自動課金されず、レート制限がかかる（基盤モデル無償プランの場合）
- 消費実績は `/budget` コマンドで実験ログから集計できる
