# レビューログ

レビューゲート（R-00〜R-04 は Claude Code のセルフレビュー、R-05 のみ taiki さんレビュー）の記録。指摘・修正・見送った指摘をすべてここに残す。

---

## R-00: Phase 0 環境セットアップ

- **実施日**: 2026-07-28
- **レビュー方式**: セルフレビュー（2026-07-28 に taiki さんから「R-00〜R-04 は Claude Code のレビュー、taiki さんが見るのは最終成果物のみ」と運用指示。D-005 参照）
- **確認内容**: 土台一式の作成完了（CLAUDE.md、管理3ファイル、コマンド5、スキル3、.env.example、.gitignore、README.md、公式 OpenAPI 仕様の保存）。sakura-ai スキルは一次情報のみで記述し、未確認事項は「要確認」と明記済み
- **判断事項の確定**:
  1. MCP 追加 → **なし**（fetch は標準 WebFetch で代替済み、github は認証済み gh CLI で代替。D-001）
  2. プラン → **基盤モデル無償プラン推奨**（最終選択はトークン発行時の taiki さん。D-002）
  3. git init → **実施**（履歴管理を開始。リモート公開は P5 まで行わない）
- **修正内容**: 新運用に合わせ CLAUDE.md・/gate コマンド・TASKS.md を改訂（U-002 は MCP 追加なし確定により削除）
- **結果**: ✅ P0 完了。P1 へ進行

---

## R-01: Phase 1 背景・課題定義

- **実施日**: 2026-07-28
- **レビュー方式**: セルフレビュー（独立サブエージェントによる批判的査読。R-02 と合同で実施、指摘合計16件）
- **成果物**: `20_design/01_background.md`（仮説 H1〜H6、実効コスト C(B) の定義、スコープ）
- **主な指摘と修正**:
  - [重大] H6 の検証指標（リクエスト単位 parse_ok）が形式間で非対称、jsonl 有利の循環論法 → **形式対称なアイテム回収率と C(B) に主指標を変更**
  - [中] 背景の「失敗→リトライで浪費」の物語と「コンテンツ失敗はリトライしない」実験設計の食い違い → 実測は非リトライ運用のコストであり、リトライ運用コストは失敗率からの外挿で示すことを明記
  - [中] H5 が反証可能性の弱い形 → 「アイテム当たりレイテンシの減少」に絞り込み
  - [軽微] B=1 基準線は素朴設計より厳しい構造化出力を課している → 限界として明記
- **見送った指摘**: なし（16件すべて反映または限界として明記）
- **結果**: ✅ P1 完了

## R-02: Phase 2 実験設計

- **実施日**: 2026-07-28
- **レビュー方式**: セルフレビュー（同上）
- **成果物**: `20_design/02_experiment_design.md`（水準・試行・採点定義・消費見積もり・予算配分）
- **主な指摘と修正**:
  - [重大] max_tokens の B 比例式は reasoning トークンの切断と B が交絡し H1/H2 を汚染 → **全水準共通の固定値（試走で校正）+ finish_reason 別カテゴリ集計**に変更
  - [重大] B≤32 では U 字の右側が観測できない恐れ（右打ち切り） → **単調減少時は B=48/96 を追試する条件分岐**を §2.4 に追加
  - [重大] H4 の検出力不足（天井効果×少標本） → 主張水準を下げ「未検出時は検出可能効果量の下限を報告」に変更、分析は復元成功アイテムに条件付け
  - [中] C(B) の trial 平均は比の平均バイアス+ゼロ除算 → プール比を主表示に、全滅 trial は C=∞ で別掲
  - [中] temperature=0 での B=1 の 3 trial は擬似反復 → **B=1 を 1 trial に削減（384 req 節約、P3 上限 1,337→914）**
  - [中] Wilson CI はアイテム反復で過小幅 → クラスターブートストラップに変更
  - [中] B=32 のリクエスト数の記載不整合（18 vs 式） → 表形式に書き直し解消
  - [軽微] 重複/未送信 id の採点規則、429カウントの要確認化、停止条件の優先順位、C(B) の形式別算出、データ難度の試走確認 → すべて反映
- **算術検算**: 全計算式を検算し正確と確認（唯一の表記不整合は上記のとおり修正）
- **見送った指摘**: なし
- **付随修正**: `.claude/skills/experiment/SKILL.md` のログスキーマに finish_reason / dup_id_count / unknown_id_count / broken_lines / fence_stripped を追加。`sakura-ai/SKILL.md` の要確認に 429カウント・reasoning 挙動を追加
- **結果**: ✅ P2 完了（**消費見積もり §2 は API 実行前に taiki さんの承認が必要**）

---

## R-03 事前コードレビュー: P3 実装（実行前）

- **実施日**: 2026-07-28
- **レビュー方式**: セルフレビュー（独立サブエージェントによる攻撃的コードレビュー。最優先の検査対象は「予算ガードの穴」と「消費記録の喪失」）
- **対象**: `30_development/` 全7ファイル + テスト（実験設計・スキル3文書と照合）
- **指摘**: 18件（重大2 / 中4 / 軽微12）
- **主な修正**:
  - [重大] 200応答のボディ破損(`resp.json()`失敗)が未捕捉 → 捕捉して `json_decode_200` として必ず記録（リトライしないコンテンツ失敗扱い）
  - [重大] 送信中の Ctrl-C 等で消費が無記録 → **write-ahead 方式**に変更（送信直前に "sent" 行を永続化。D-009）+ BaseException 捕捉で記録後に再送出
  - [中] 解析例外でログ喪失 → try/finally で必ずログ。choices/message の型ガード追加
  - [中] smoke の生応答が未保存・reasoning_effort 未検証 → raw_content/usage/message_keys の保存と受理プローブを追加
  - [中] 再開時のパラメータ不一致が素通り → meta.json と突合し不一致なら起動拒否
  - [中] items/labels の id 集合不一致を未検証 → `load_all()` に相互検証を追加（難度差し替え時の防護）
  - [軽微12件] cap合計915→914に修正(p3-003: 119→118)、リトライ対象を429/5xx/応答なしに限定、Retry-After優先の是正、--max-tokens必須化、非200ボディの記録、集計規約の明文化（unit毎に最後の成功のみ）ほか、全件反映
- **テスト**: 18件 green（write-ahead の2行構成・記録完全性・cap停止・再開・パラメータ不一致拒否を含む）
- **問題なしと確認された点**: 予算ガードの基本設計、429連続停止、バックオフ、計画数の設計一致（696/108）、採点規則、シード共有・ローテーション、tally の月フィルタ
- **結果**: ✅ 実装完了。**API 実行（p3-001〜）は U-001 完了と消費見積もり承認が前提**

---

## R-03 / R-04: Phase 3 実験実行・Phase 4 考察

- **実施日**: 2026-07-30
- **レビュー方式**: セルフレビュー（実測データとの突き合わせ）
- **実行した run**: p3-001 試走(18) / p3-000 探索(18) / p3-000b 探索(30) / p3-100 本実験(288) / p3-200 追試(384) = **合計 738 req**（承認上限 914 req 内、7月枠の24.7%）
- **設計変更**: 探索の結果、当初仮説（U字カーブ）の前提が崩れたため設計を全面改訂（D-012）。承認済み消費量の範囲内で実施
- **検証結果**: H1'〜H6' すべて検証完了。当初 H4（後方ほど誤答が増える）は誤答がほぼ発生せず、位置効果は「欠落」として現れた
- **自己指摘と修正**:
  - p3-100 で「切断時の JSONL 回収率 23.2%」と結論したが、p3-200 で崩壊モードを分解したところ、JSONL が効くのは「出力途中で切断」（12%）のみで、支配的な「reasoning枯渇」（88%）では形式に関係なく全損すると判明 → 考察を精密化し、両方の数値と条件を明記
  - 図の日本語が豆腐になる不具合を検出 → `font.family` にリストを直接渡す必要があると判明し修正
  - 図の逐次ランプが配色検証で FAIL → 隣接ペアの判別基準を満たすランプに差し替え（通常視ΔE 15.3）
- **成果物**: `40_test/analysis/01_findings.md`（考察）、`p3-100_summary.md` `p3-200_summary.md`（集計）、図6枚
- **結果**: ✅ P3・P4 完了。P5 へ進行

---

## R-05: Phase 5 最終成果物（taiki さんレビュー）

- **依頼日**: 2026-07-30
- **成果物**: `50_output/qiita_draft.md`（約10,700字、図5枚）
- **taiki さんの回答**:
  1. タイトル → **B案を採用**（「無償枠3,000リクエストで28万件処理できる — さくらのAI Engine でバッチサイズの限界を実測した」）
  2. 記事の長さ → そのまま（削減しない）
  3. 付録のセットアップ手順 → 残す
  4. **指摘: T-401 の状態はどうなっているか**
- **修正内容**:
  - タイトルをB案に差し替え。ただし28.8万件は外挿値のため、語尾を「処理する」→「処理できる」に調整し、TL;DR冒頭・結果1・制約の3箇所で外挿であることを明示（qiita-writing規約「推測値には明示的にラベル」）
  - **T-401 の二重登録を解消**。私の TASKS.md 更新時のミスで、完了済み行（✅）と古い未着手のプレースホルダ行（⬜）が同一IDで併存していた。実作業は完了済み（成果物は実在）のため、古い行を削除。全タスクIDの重複検査も実施し、他に重複がないことを確認
- **taiki さんの指摘の意義**: タスクリストと実態の一致（行動原則4）が崩れていたのを検出された。今後 TASKS.md を更新する際は、行の追加ではなく既存行の置換であることを確認する
- **結果**: 修正反映済み。再レビュー待ち
